Vagrant.configure("2") do |config|
    config.vm.box = "jouramie/CS20-workshop-OS"
    config.vm.box_version = "1.0"

    config.vm.provider "virtualbox" do |vbox|
        vbox.gui = false
      end
  end